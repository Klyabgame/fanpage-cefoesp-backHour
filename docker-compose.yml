version: '3'

services:
  db:
    image: mongo:6
    restart: always
    ports:
      - 27018:27017
    environment:
      MONGODB_DATABASE: back-fanpage-cefoesp
    volumes:
      - ./mongo:/data/db

      
